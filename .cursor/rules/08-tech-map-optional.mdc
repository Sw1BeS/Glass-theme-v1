---
description: Техническая карта (подключать при правках цветов/скриптов)
globs:
  - "assets/**/*.js"
  - "assets/**/*.css"
alwaysApply: false
---
# IS BEAUTY LUX - ТЕХНИЧЕСКИЕ ДЕТАЛИ И КОД-ГАЙД

## 🎨 ТОЧНОЕ РАСПОЛОЖЕНИЕ КОДА ЦВЕТОВЫХ СХЕМ

**Файл:** `assets/theme-switcher.js`
**Строки:** 570-697
**Функция:** `updateShopifyThemeColors(scheme)`

**Структура функции:**

```javascript
updateShopifyThemeColors(scheme) {
  // Создание динамических стилей
  const style = document.createElement('style');
  style.id = 'dynamic-theme-colors';
  style.textContent = `
    :root { /* CSS переменные */ }
    body { /* Основные стили body */ }
    .section { /* Стили секций */ }
    /* Кастомные секции начинаются с строки 636 */
  `;

  // Удаление старых стилей и добавление новых
  const existingStyle = document.getElementById('dynamic-theme-colors');
  if (existingStyle) existingStyle.remove();
  document.head.appendChild(style);
}
```

## 🏗️ КАК ДОБАВИТЬ НОВУЮ СЕКЦИЮ - ПОШАГОВО

**Шаг 1:** Найти строку 636 в assets/theme-switcher.js (после `p, span, div` стилей)

**Шаг 2:** Добавить блок стилей:

```javascript
/* Название Секции - Custom styling */
.main-selector {
  background: ${scheme.colors['--color-background']} !important;
  color: ${scheme.colors['--color-foreground']} !important;
}

.child-selector {
  border-color: ${scheme.colors['--color-border']} !important;
}
```

**Шаг 3:** Обязательно использовать `!important` для переопределения Shopify стилей

**Шаг 4:** Тестировать с каждой из 6 цветовых схем

## 📋 ДОСТУПНЫЕ ЦВЕТОВЫЕ ПЕРЕМЕННЫЕ

**Фоновые цвета:**

- `${scheme.colors['--color-background']}` - основной фон
- `${scheme.colors['--color-secondary-bg']}` - вторичный фон
- `${scheme.colors['--color-tertiary-bg']}` - третичный фон
- `${scheme.colors['--color-quaternary-bg']}` - четвертичный фон

**Акцентные цвета:**

- `${scheme.colors['--color-primary']}` - основной акцент
- `${scheme.colors['--color-primary-hover']}` - hover состояние
- `${scheme.colors['--color-accent']}` - дополнительный акцент

**Текстовые цвета:**

- `${scheme.colors['--color-foreground']}` - основной текст
- `${scheme.colors['--color-foreground-light']}` - светлый текст
- `${scheme.colors['--color-foreground-muted']}` - приглушенный текст

**Границы:**

- `${scheme.colors['--color-border']}` - основные границы
- `${scheme.colors['--color-border-light']}` - светлые границы

## 🎯 ПРИМЕРЫ РЕАЛЬНЫХ ИНТЕГРАЦИЙ

**How to Use секция (строки 636-687):**

```javascript
/* How to Use Section - Custom styling */
.how-to-use-section {
  background: linear-gradient(135deg, ${scheme.colors['--color-secondary-bg']} 0%, ${scheme.colors['--color-tertiary-bg']} 100%) !important;
}

.step-number {
  background: linear-gradient(135deg, ${scheme.colors['--color-primary']}, ${scheme.colors['--color-accent']}) !important;
}

.step-tip {
  background: ${scheme.colors['--color-primary']}1a !important;
  border-left-color: ${scheme.colors['--color-primary']} !important;
}
```

**Заметки:**

- `1a` в конце цвета = 10% прозрачность в hex
- `linear-gradient` для красивых переходов
- Всегда `!important` для переопределения

## 🔧 СИСТЕМА ЭФФЕКТОВ

**Местоположение:** `assets/theme-switcher.js`
**Объект эффектов:** `this.effects` (строка ~115)

**Доступные эффекты:**

```javascript
this.effects = {
  animations: false,
  particles: false,
  spotlight: false,
  shimmer: false,
}
```

**Функции управления:**

- `toggleEffect(effect)` - основная функция переключения
- `toggleAnimations()` - управление анимациями
- `toggleParticles()` - частицы
- `toggleSpotlight()` - прожектор
- `toggleShimmer()` - мерцание

## 📱 МОБИЛЬНАЯ АДАПТИВНОСТЬ

**В секциях использовать:**

```css
@media (max-width: 768px) {
  .how-to-step {
    flex-direction: column !important;
    text-align: center;
  }
}
```

**Принципы:**

- Вертикальное расположение на мобильных
- Центрирование контента
- Упрощение анимаций
- Скрытие декоративных элементов

## 🎨 АНИМАЦИИ И ЭФФЕКТЫ

**CSS Keyframes в How to Use:**

```css
@keyframes pulse {
  0%,
  100% {
    transform: scale(1);
    opacity: 0.3;
  }
  50% {
    transform: scale(1.1);
    opacity: 0.5;
  }
}

@keyframes fillProgress {
  0% {
    height: 0%;
  }
  50% {
    height: 100%;
  }
  100% {
    height: 0%;
  }
}
```

**Применение:**

- `animation: pulse 2s infinite;`
- `animation: fillProgress 3s ease-in-out infinite;`

## 🔍 ОТЛАДКА И ТЕСТИРОВАНИЕ

**Проверить работу схем:**

1. Открыть DevTools
2. Найти `#dynamic-theme-colors` в `<head>`
3. Проверить применение CSS переменных
4. Убедиться в наличии `!important`

**Типичные проблемы:**

- Отсутствует `!important`
- Неправильное имя CSS класса
- CSS переменная не определена в схеме
- Конфликт с Shopify стилями

## 📋 ЧЕКЛИСТ ДОБАВЛЕНИЯ/УДАЛЕНИЯ БЛОКОВ

### ✅ ДОБАВЛЕНИЕ НОВОЙ СЕКЦИИ:

**1. Создать файл секции:**

- `sections/название-секции.liquid`
- Добавить HTML структуру
- Добавить CSS стили в `{% stylesheet %}`
- Добавить схему в `{% schema %}`

**2. Добавить в templates/index.json:**

- Добавить секцию в основной объект
- Добавить в `section_order` массив
- Настроить блоки и их настройки

**3. Интегрировать с цветовыми схемами:**

- Добавить стили в `assets/theme-switcher.js`
- Функция `updateShopifyThemeColors()` (строки 636+)
- Использовать `${scheme.colors['--color-*']}`
- Добавить `!important` для переопределения

**4. Проверить типы блоков:**

- Убедиться что тип блока соответствует схеме
- Проверить в `{% schema %}` секции
- Использовать правильные `id` и `label`

### ❌ УДАЛЕНИЕ СЕКЦИИ:

**1. Удалить из templates/index.json:**

- Удалить секцию из основного объекта
- Удалить из `section_order` массива
- Проверить что нет ссылок на секцию

**2. Удалить файл секции:**

- `sections/название-секции.liquid`
- Удалить файл полностью

**3. Удалить из цветовых схем:**

- Удалить стили из `assets/theme-switcher.js`
- Удалить CSS классы секции
- Очистить неиспользуемые переменные

### ⚠️ ТИПИЧНЫЕ ОШИБКИ:

**Ошибка валидации типов блоков:**

- Проверить соответствие типа блока схеме
- Убедиться что тип определен в `{% schema %}`
- Использовать правильные `id` и `label`

**Дублирование секций:**

- Проверить что нет двух секций с одинаковым `type`
- Убедиться что в `section_order` нет дублей
- Очистить старые версии секций

**Конфликты стилей:**

- Проверить уникальность CSS классов
- Использовать `!important` для переопределения
- Тестировать с разными цветовыми схемами

### 🔧 ПРОВЕРКА ПОСЛЕ ИЗМЕНЕНИЙ:

1. **Валидация JSON:**

   - Проверить синтаксис templates/index.json
   - Убедиться что все блоки имеют правильные типы
   - Проверить соответствие схеме секций

2. **Тестирование цветовых схем:**

   - Переключить все 6 цветовых схем
   - Проверить что стили применяются
   - Убедиться в отсутствии конфликтов

3. **Мобильная адаптивность:**

   - Проверить на разных размерах экрана
   - Убедиться что контент не переполняется
   - Проверить работу анимаций

4. **Интеграция с Shopify:**
   - Проверить работу в Theme Editor
   - Убедиться что настройки сохраняются
   - Проверить предварительный просмотр

## 🎯 СТРУКТУРА ФАЙЛОВ

**Основные файлы стилей:**

- `assets/custom-styles.css` - кастомные стили проекта
- `snippets/color-schemes.liquid` - Shopify цветовые схемы
- `layout/theme.liquid` - основной макет

**Секции:**

- `sections/how-to-use.liquid` - кастомная секция "Как использовать"
- `sections/faq.liquid` - кастомная секция FAQ
- `sections/hero.liquid` - hero секция
- `sections/section.liquid` - базовая секция

**JavaScript:**

- `assets/theme-switcher.js` - переключатель тем и эффектов
- `assets/theme-editor.js` - редактор тем

## 🎨 БРЕНДОВЫЕ ЦВЕТА IS BEAUTY LUX

- Rose: #bf8a7a
- Porcelain: #ece7d7
- Peach: #dab4a6
- Charcoal: #5a5e5a

## 📋 ШАБЛОНЫ И НАСТРОЙКИ

**Настройки темы:** `config/settings_data.json`
**Шаблоны страниц:** `templates/`
**Блоки:** `blocks/`
**Сниппеты:** `snippets/`

## 🎨 HOW TO USE СЕКЦИЯ

**Файл:** `sections/how-to-use.liquid`
**CSS классы для стилизации:**

- `.how-to-use-section` - основная секция
- `.how-to-use-title/.how-to-use-subtitle` - заголовки
- `.how-to-step` - карточки шагов
- `.step-number` - номера шагов
- `.step-heading/.step-description` - текст шагов
- `.step-tip` - подсказки
- `.step-duration` - время выполнения
- `.step-placeholder` - плейсхолдеры

**Схема блоков:**

```json
{
  "type": "step",
  "settings": {
    "heading": "Заголовок шага",
    "text": "Описание",
    "icon": "🧴",
    "tip": "Совет",
    "duration": "1-2 минуты"
  }
}
```

## 🎨 FAQ СЕКЦИЯ

**Файл:** `sections/faq.liquid`
**CSS классы для стилизации:**

- `.faq-section` - основная секция
- `.faq-title/.faq-subtitle` - заголовки
- `.faq-item` - карточки вопросов
- `.faq-number` - номера вопросов
- `.question-text` - текст вопроса
- `.answer-content` - текст ответа
- `.additional-info` - дополнительная информация

**Схема блоков:**

```json
{
  "type": "faq_item",
  "settings": {
    "question": "Вопрос",
    "answer": "Ответ",
    "additional_info": "Дополнительная информация"
  }
}
```
