{% comment %}
  Simple Header: Header IS the glass, content sits on top
  Layer structure: Background -> Orbs -> Glass (header) -> Content
{% endcomment %}

{% liquid
  assign logo_width = section.settings.logo_width | default: '120px'
  assign logo_width_mobile = section.settings.logo_width_mobile | default: '100px'
  assign logo_height = section.settings.logo_height | default: '40px'
  assign logo_height_mobile = section.settings.logo_height_mobile | default: '35px'
  assign menu_items = section.settings.menu.links
  assign show_search = section.settings.show_search
  assign show_cart = section.settings.show_cart
  assign show_account = section.settings.show_account
  assign sticky_behavior = section.settings.enable_sticky_header | default: 'always'

  assign glass_enabled = section.settings.enable_glass
  if glass_enabled == null
    assign glass_enabled = settings.enable_glass_effect
  endif
  assign glass_tone = section.settings.glass_tone | default: 'auto'
  assign glass_elevation = section.settings.glass_elevation | default: 'medium'

  assign glass_blur_preset = section.settings.glass_blur_preset | default: 'global'
  assign glass_blur_custom = section.settings.glass_blur_custom | default: 20
  assign glass_blur = null
  case glass_blur_preset
    when 'soft'
      assign glass_blur = 14
    when 'balanced'
      assign glass_blur = 20
    when 'bold'
      assign glass_blur = 28
    when 'custom'
      assign glass_blur = glass_blur_custom
  endcase

  assign glass_opacity = section.settings.glass_opacity | default: 82
  assign glass_border_opacity = section.settings.glass_border_opacity | default: 35
  assign glass_tint = section.settings.glass_tint | default: 18
  assign shadow_strength = section.settings.shadow_strength | default: 38
  assign tint_inverse = 100 | minus: glass_tint
  assign padding_inline = section.settings.padding_inline | default: 28
  assign padding_block = section.settings.padding_block | default: 16
  assign corner_radius = section.settings.corner_radius | default: 32
  assign content_gap = section.settings.content_gap | default: 18
  assign content_alignment = section.settings.content_alignment | default: 'space-between'

  # Mobile settings
  assign padding_inline_mobile = section.settings.padding_inline_mobile | default: 20
  assign padding_block_mobile = section.settings.padding_block_mobile | default: 12
  assign corner_radius_mobile = section.settings.corner_radius_mobile | default: 24
  assign content_gap_mobile = section.settings.content_gap_mobile | default: 14

  # Animation settings
  assign enable_header_animation = section.settings.enable_header_animation | default: true
  assign header_animation_duration = section.settings.header_animation_duration | default: 300

  # Apply animation duration to CSS
  if enable_header_animation
    assign animation_duration_css = header_animation_duration | append: 'ms'
  else
    assign animation_duration_css = '0ms'
  endif

  assign header_height_raw = section.settings.header_height | default: '60px'
  assign header_width_raw = section.settings.header_width | default: '1200px'

  assign header_height_value = header_height_raw | strip
  if header_height_value == ''
    assign header_height_value = '60px'
  endif
  assign header_height_lower = header_height_value | downcase
  if header_height_lower contains 'px' or header_height_lower contains 'rem' or header_height_lower contains 'em' or header_height_lower contains 'vh' or header_height_lower contains 'vw' or header_height_lower contains '%' or header_height_lower contains 'ch' or header_height_lower contains 'calc' or header_height_lower contains 'auto'
    assign header_height_css = header_height_value
  else
    assign header_height_css = header_height_value | append: 'px'
  endif

  assign header_width_value = header_width_raw | strip
  if header_width_value == ''
    assign header_width_value = '1200px'
  endif
  assign header_width_lower = header_width_value | downcase
  if header_width_lower contains 'px' or header_width_lower contains 'rem' or header_width_lower contains 'em' or header_width_lower contains 'vh' or header_width_lower contains 'vw' or header_width_lower contains '%' or header_width_lower contains 'ch' or header_width_lower contains 'calc' or header_width_lower contains 'auto'
    assign header_width_css = header_width_value
  else
    assign header_width_css = header_width_value | append: 'px'
  endif
%}

{% capture header_style %}
  {% if glass_blur != blank %}
    --glass-local-blur: {{ glass_blur }}px;
  {% endif %}
  --header-glass-opacity: {{ glass_opacity }}%;
  --header-border-opacity: {{ glass_border_opacity }}%;
  --header-tint-strength: {{ glass_tint }}%;
  --header-shadow-strength: {{ shadow_strength }}%;
  --header-spacing-inline: {{ padding_inline }}px;
  --header-spacing-block: {{ padding_block }}px;
  --header-radius: {{ corner_radius }}px;
  --header-gap: {{ content_gap }}px;
  --header-justify: {{ content_alignment }};
  --header-spacing-inline-mobile: {{ padding_inline_mobile }}px;
  --header-spacing-block-mobile: {{ padding_block_mobile }}px;
  --header-radius-mobile: {{ corner_radius_mobile }}px;
  --header-gap-mobile: {{ content_gap_mobile }}px;
  --header-animation-duration: {{ animation_duration_css }};
  --header-surface-color: color-mix(in srgb, var(--glass-surface-color, rgba(255, 255, 255, 0.82)) {{ glass_opacity }}%, transparent);
  --header-surface-color: color-mix(in srgb, var(--header-surface-color) {{ tint_inverse }}%, var(--glass-highlight-color, var(--color-primary, #bf8a7a)) {{ glass_tint }}%);
  --header-border-color: color-mix(in srgb, var(--glass-surface-border-color, rgba(255, 255, 255, 0.4)) {{ glass_border_opacity }}%, transparent);
  --header-shadow-color: color-mix(in srgb, var(--glass-shadow-color, rgba(17, 23, 34, 0.2)) {{ shadow_strength }}%, transparent);
{% endcapture %}
{% assign header_style_trimmed = header_style | strip %}

<header
  class="header{% if glass_enabled %} glass{% endif %}"
  data-sticky="{{ sticky_behavior }}"
  {% if glass_enabled %}
    data-glass-tone="{{ glass_tone }}"
    data-glass-elevation="{{ glass_elevation }}"
  {% endif %}
  {% unless header_style_trimmed == '' %}
    style="{{ header_style_trimmed }}"
  {% endunless %}
>
  <!-- Logo - прямо в хедере -->
  <a href="{{ routes.root_url }}" class="logo-link">
    {% if section.settings.logo != blank %}
      <img
        src="{{ section.settings.logo | image_url: width: 400 }}"
        alt="{{ shop.name }}"
        class="logo-image"
        width="400"
        height="auto"
        style="width: {{ logo_width }}; height: {{ logo_height }}; object-fit: contain;"
        loading="eager"
      >
    {% else %}
      <span class="logo-text">{{ shop.name }}</span>
    {% endif %}
  </a>

  <!-- Desktop Menu - прямо в хедере -->
  <nav class="menu-group desktop-only" aria-label="Primary">
    {% for link in menu_items %}
      <a href="{{ link.url }}" class="menu-link">{{ link.title }}</a>
    {% endfor %}
  </nav>

  <div class="header-right">
    <div class="action-group">
      {% if show_search %}
        <a href="{{ routes.search_url }}" class="action-btn" aria-label="Search">🔍</a>
      {% endif %}
      {% if show_account %}
        <a href="{{ routes.account_url }}" class="action-btn" aria-label="Account">👤</a>
      {% endif %}
      {% if show_cart %}
        <a href="{{ routes.cart_url }}" class="action-btn" aria-label="Cart">
          🛒 <span class="cart-count">{{ cart.item_count }}</span>
        </a>
      {% endif %}
    </div>

    <!-- Mobile Menu Toggle -->
    <button
      class="mobile-toggle mobile-only"
      type="button"
      aria-expanded="false"
      aria-controls="HeaderMobileMenu-{{ section.id }}"
      data-mobile-toggle
    >
      ☰
    </button>
  </div>

  <!-- Mobile Menu -->
  <div class="mobile-menu" id="HeaderMobileMenu-{{ section.id }}" data-mobile-menu>
    {% for link in menu_items %}
      <a href="{{ link.url }}" class="mobile-menu-link">{{ link.title }}</a>
    {% endfor %}
  </div>
</header>

<style>
  #shopify-section-{{ section.id }} {
    --header-logo-width: {{ logo_width }};
    --header-logo-width-mobile: {{ logo_width_mobile }};
    --header-logo-height: {{ logo_height }};
    --header-logo-height-mobile: {{ logo_height_mobile }};
    --header-height: {{ header_height_css }};
    --header-width: {{ header_width_css }};
  }

  #shopify-section-{{ section.id }} .header {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: var(--header-justify, space-between);
    flex-wrap: wrap;
    gap: var(--header-gap, clamp(0.75rem, 3vw, 1.6rem));
    width: min(100%, var(--header-width, 1200px));
    min-height: var(--header-height, 60px);
    margin: clamp(0.6rem, 2.2vw, 1.5rem) auto;
    padding: var(--header-spacing-block, clamp(0.65rem, 2vw, 1rem)) var(--header-spacing-inline, clamp(1rem, 3vw, 2rem));
    border-radius: var(--header-radius, clamp(1.8rem, 4vw, 3.2rem));
    box-sizing: border-box;
    z-index: 20;
    background: var(--header-surface-color, color-mix(in srgb, var(--glass-surface-color, rgba(255, 255, 255, 0.82)) var(--header-glass-opacity, 82%), transparent));
    border: 1px solid var(--header-border-color, color-mix(in srgb, var(--glass-surface-border-color, rgba(255, 255, 255, 0.4)) 35%, transparent));
    box-shadow: 0 18px 42px var(--header-shadow-color, color-mix(in srgb, var(--glass-shadow-color, rgba(17, 23, 34, 0.2)) 35%, transparent));
  }

  #shopify-section-{{ section.id }} .header > * {
    flex-shrink: 0;
  }

  #shopify-section-{{ section.id }} .logo-link {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--color-foreground, #2c2c2c);
    transition: transform 0.25s ease;
    min-width: 0;
  }

  #shopify-section-{{ section.id }} .logo-link:hover {
    transform: translateY(-2px);
  }

  #shopify-section-{{ section.id }} .logo-image {
    width: var(--header-logo-width) !important;
    height: var(--header-logo-height) !important;
    max-width: 100% !important;
    max-height: none !important;
    object-fit: contain !important;
  }

  #shopify-section-{{ section.id }} .logo-text {
    font-size: clamp(1.35rem, 2.6vw, 1.65rem);
    font-weight: 700;
    color: var(--color-foreground, #2c2c2c);
  }

  #shopify-section-{{ section.id }} .menu-group {
    flex: 1 1 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: clamp(0.75rem, 2.5vw, 1.65rem);
    min-width: min(320px, 100%);
  }

  #shopify-section-{{ section.id }} .menu-link {
    color: var(--color-foreground, #2c2c2c);
    text-decoration: none;
    font-weight: 600;
    padding: 0.55rem 0.9rem;
    border-radius: 999px;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    white-space: nowrap;
  }

  #shopify-section-{{ section.id }} .menu-link:hover,
  #shopify-section-{{ section.id }} .menu-link:focus-visible {
    background: color-mix(in srgb, var(--glass-surface-color, rgba(255, 255, 255, 0.8)) 70%, transparent);
    box-shadow: 0 12px 26px rgba(20, 26, 34, 0.12);
    transform: translateY(-2px);
  }

  #shopify-section-{{ section.id }} .header-right {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    margin-left: auto;
    flex-shrink: 0;
    flex-wrap: nowrap;
  }

  #shopify-section-{{ section.id }} .action-group {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
  }

  #shopify-section-{{ section.id }} .action-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 999px;
    border: none;
    background: transparent;
    color: var(--color-foreground, #2c2c2c);
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    position: relative;
    cursor: pointer;
  }

  #shopify-section-{{ section.id }} .action-btn:hover,
  #shopify-section-{{ section.id }} .action-btn:focus-visible {
    background: color-mix(in srgb, var(--glass-surface-color, rgba(255, 255, 255, 0.78)) 72%, transparent);
    transform: translateY(-2px);
    box-shadow: 0 12px 26px rgba(20, 26, 34, 0.12);
    outline: none;
  }

  #shopify-section-{{ section.id }} .cart-count {
    position: absolute;
    top: -4px;
    right: -4px;
    min-width: 18px;
    height: 18px;
    padding: 0 4px;
    border-radius: 999px;
    background: var(--color-primary, #bf8a7a);
    color: #fff;
    font-size: 0.7rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  #shopify-section-{{ section.id }} .mobile-toggle {
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.1));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.22));
    border-radius: 12px;
    width: 42px;
    height: 42px;
    font-size: 1.1rem;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  #shopify-section-{{ section.id }} .mobile-toggle:hover,
  #shopify-section-{{ section.id }} .mobile-toggle:focus-visible {
    background: color-mix(in srgb, var(--glass-surface-color, rgba(255, 255, 255, 0.8)) 80%, transparent);
  }

  #shopify-section-{{ section.id }} .mobile-menu {
    position: absolute;
    left: 0;
    right: 0;
    top: calc(100% + 0.5rem);
    display: none;
    flex-direction: column;
    gap: 0.35rem;
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.85));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.35));
    border-radius: 18px;
    padding: 1rem;
    box-shadow: 0 24px 40px var(--glass-shadow-color, rgba(20, 26, 34, 0.16));
    opacity: 0;
    transform: translateY(-12px);
    pointer-events: none;
    max-height: 0;
    overflow: hidden;
    transition: opacity 0.25s ease, transform 0.25s ease, max-height 0.25s ease;
    z-index: 5;
  }

  #shopify-section-{{ section.id }} .mobile-menu.active {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
    max-height: 420px;
  }

  #shopify-section-{{ section.id }} .mobile-menu-link {
    display: block;
    padding: 0.75rem 1rem;
    border-radius: 12px;
    text-decoration: none;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    font-weight: 500;
    transition: background 0.2s ease, transform 0.2s ease;
  }

  #shopify-section-{{ section.id }} .mobile-menu-link:hover,
  #shopify-section-{{ section.id }} .mobile-menu-link:focus-visible {
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.8));
    transform: translateX(4px);
  }

  #shopify-section-{{ section.id }} .mobile-menu-link:last-child {
    margin-bottom: 0;
  }

  #shopify-section-{{ section.id }} .mobile-only {
    display: none;
  }

  @media (max-width: 1023px) {
    #shopify-section-{{ section.id }} .mobile-menu {
      display: flex;
    }

    #shopify-section-{{ section.id }} .header {
      gap: var(--header-gap-mobile, clamp(0.6rem, 2vw, 1.1rem));
      padding: var(--header-spacing-block-mobile, clamp(0.5rem, 2vw, 0.75rem)) var(--header-spacing-inline-mobile, clamp(0.85rem, 4vw, 1.25rem));
      width: calc(100% - clamp(1rem, 4vw, 2.5rem));
      flex-wrap: nowrap;
      min-height: 60px;
    }

    #shopify-section-{{ section.id }} .menu-group.desktop-only {
      display: none !important;
    }

    #shopify-section-{{ section.id }} .mobile-only {
      display: inline-flex;
    }

    #shopify-section-{{ section.id }} .header-right {
      gap: 0.4rem;
      align-items: center;
    }
  }

  @media (max-width: 680px) {
    #shopify-section-{{ section.id }} .header {
      border-radius: var(--header-radius-mobile, clamp(1.2rem, 5vw, 2rem));
    }

    #shopify-section-{{ section.id }} .header-right {
      gap: 0.3rem;
    }

    #shopify-section-{{ section.id }} .logo-image {
      width: var(--header-logo-width-mobile) !important;
      height: var(--header-logo-height-mobile) !important;
    }

    #shopify-section-{{ section.id }} .logo-text {
      font-size: clamp(1.2rem, 5vw, 1.45rem);
    }

    #shopify-section-{{ section.id }} .action-btn,
    #shopify-section-{{ section.id }} .mobile-menu {
      right: 0;
      left: 0;
      width: 100%;
    }
  }

  #shopify-section-{{ section.id }} .header[data-sticky="always"],
  #shopify-section-{{ section.id }} .header[data-sticky="scroll-up"] {
    position: fixed;
    top: clamp(0.6rem, 2vw, 1.25rem);
    left: 50%;
    transform: translateX(-50%);
    width: min(100% - clamp(1rem, 4vw, 3rem), var(--header-width, 1200px));
    margin: 0;
  }

  #shopify-section-{{ section.id }} .header[data-sticky="scroll-up"] {
    transform: translate(-50%, -120%);
    transition: transform 0.35s ease;
  }

  #shopify-section-{{ section.id }} .header[data-sticky="scroll-up"].visible {
    transform: translate(-50%, 0);
  }

  #shopify-section-{{ section.id }} .header[data-sticky="never"] {
    position: relative;
    top: auto;
    left: auto;
    transform: none;
    width: min(100%, var(--header-width, 1200px));
  }

  @media (max-width: 1023px) {
    #shopify-section-{{ section.id }} .header[data-sticky="always"],
    #shopify-section-{{ section.id }} .header[data-sticky="scroll-up"] {
      width: calc(100% - clamp(1rem, 4vw, 2.25rem));
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const section = document.getElementById('shopify-section-{{ section.id }}');
    if (!section) {
      return;
    }

    const mobileToggle = section.querySelector('[data-mobile-toggle]');
    const mobileMenu = section.querySelector('[data-mobile-menu]');

    if (mobileToggle && mobileMenu) {
      const toggleMenu = function () {
        const isOpen = mobileMenu.classList.toggle('active');
        mobileToggle.classList.toggle('active', isOpen);
        mobileToggle.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
      };

      mobileToggle.addEventListener('click', toggleMenu);

      mobileMenu.querySelectorAll('a').forEach(function (link) {
        link.addEventListener('click', function () {
          if (mobileMenu.classList.contains('active')) {
            toggleMenu();
          }
        });
      });
    }

    const stickyHeader = section.querySelector('.header[data-sticky="scroll-up"]');
    if (stickyHeader) {
      let lastScrollY = window.scrollY;
      const handleScroll = function () {
        const currentScrollY = window.scrollY;
        if (currentScrollY > lastScrollY && currentScrollY > 100) {
          stickyHeader.classList.remove('visible');
        } else {
          stickyHeader.classList.add('visible');
        }
        lastScrollY = currentScrollY;
      };

      handleScroll();
      window.addEventListener('scroll', handleScroll, { passive: true });
    }

    if (window.Shopify && window.Shopify.designMode) {
      document.addEventListener('shopify:section:load', function (event) {
        if (event.detail.sectionId !== '{{ section.id }}') {
          return;
        }
        const updatedSection = document.getElementById('shopify-section-{{ section.id }}');
        if (!updatedSection) {
          return;
        }
        const logoImage = updatedSection.querySelector('.logo-image');
        if (logoImage) {
          logoImage.style.height = 'var(--header-logo-height)';
        }
      });
    }
  });
</script>

{% schema %}
{
  "name": "Simple Header",
  "settings": [
    {
      "type": "header",
      "content": "Header Size"
    },
    {
      "type": "text",
      "id": "header_height",
      "label": "Header height",
      "default": "60px",
      "info": "Accepts CSS lengths such as 60px, 4.5rem, 50vh. Enter digits only to use pixels."
    },
    {
      "type": "text",
      "id": "header_width",
      "label": "Header max width",
      "default": "1200px",
      "info": "Accepts CSS lengths (e.g. 1200px, 85vw, calc(100% - 3rem))."
    },
    {
      "type": "header",
      "content": "Logo Settings"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "logo_width",
      "label": "Logo Width (Desktop)",
      "default": "120px",
      "info": "Any CSS value: 120px, 200px, 50%, auto, 10rem, etc."
    },
    {
      "type": "text",
      "id": "logo_width_mobile",
      "label": "Logo Width (Mobile)",
      "default": "100px",
      "info": "Any CSS value: 100px, 150px, 50%, auto, 8rem, etc."
    },
    {
      "type": "text",
      "id": "logo_height",
      "label": "Logo Height (Desktop)",
      "default": "40px",
      "info": "Any CSS value: 40px, 80px, 50%, auto, 3rem, etc."
    },
    {
      "type": "text",
      "id": "logo_height_mobile",
      "label": "Logo Height (Mobile)",
      "default": "35px",
      "info": "Any CSS value: 35px, 60px, 50%, auto, 2.5rem, etc."
    },
    {
      "type": "header",
      "content": "Menu Settings"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "header",
      "content": "Action Buttons"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show Search",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_cart",
      "label": "Show Cart",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "Show Account",
      "default": true
    },
    {
      "type": "header",
      "content": "Behavior"
    },
    {
      "type": "select",
      "id": "enable_sticky_header",
      "label": "Sticky Header",
      "options": [
        {
          "value": "always",
          "label": "Always"
        },
        {
          "value": "scroll-up",
          "label": "On Scroll Up"
        },
        {
          "value": "never",
          "label": "Never"
        }
      ],
      "default": "always"
    },
    {
      "type": "header",
      "content": "Glass styling"
    },
    {
      "type": "checkbox",
      "id": "enable_glass",
      "label": "Use glassmorphism background",
      "default": true,
      "info": "Falls back to the global glass setting when left unchecked in existing themes."
    },
    {
      "type": "select",
      "id": "glass_tone",
      "label": "Glass tone",
      "options": [
        {
          "value": "auto",
          "label": "Auto"
        },
        {
          "value": "light",
          "label": "Light"
        },
        {
          "value": "dark",
          "label": "Dark"
        }
      ],
      "default": "auto"
    },
    {
      "type": "select",
      "id": "glass_elevation",
      "label": "Glass elevation",
      "options": [
        {
          "value": "low",
          "label": "Low"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "high",
          "label": "High"
        }
      ],
      "default": "medium"
    },
    {
      "type": "select",
      "id": "glass_blur_preset",
      "label": "Blur strength",
      "options": [
        {
          "value": "global",
          "label": "Use theme default"
        },
        {
          "value": "soft",
          "label": "Soft (14px)"
        },
        {
          "value": "balanced",
          "label": "Balanced (20px)"
        },
        {
          "value": "bold",
          "label": "Bold (28px)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "global"
    },
    {
      "type": "range",
      "id": "glass_blur_custom",
      "label": "Custom blur (px)",
      "min": 4,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 20,
      "info": "Used when blur strength is set to Custom."
    },
    {
      "type": "header",
      "content": "Glass intensity"
    },
    {
      "type": "range",
      "id": "glass_opacity",
      "label": "Surface opacity",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 82
    },
    {
      "type": "range",
      "id": "glass_border_opacity",
      "label": "Border visibility",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 35
    },
    {
      "type": "range",
      "id": "glass_tint",
      "label": "Tint mix",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 18
    },
    {
      "type": "range",
      "id": "shadow_strength",
      "label": "Shadow strength",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 38
    },
    {
      "type": "header",
      "content": "Spacing & layout"
    },
    {
      "type": "range",
      "id": "padding_inline",
      "label": "Horizontal padding",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "padding_block",
      "label": "Vertical padding",
      "min": 0,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "corner_radius",
      "label": "Corner radius",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "content_gap",
      "label": "Gap between groups",
      "min": 0,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 18
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Content distribution",
      "options": [
        {
          "value": "flex-start",
          "label": "Packed left"
        },
        {
          "value": "center",
          "label": "Centered"
        },
        {
          "value": "flex-end",
          "label": "Packed right"
        },
        {
          "value": "space-between",
          "label": "Spread"
        },
        {
          "value": "space-around",
          "label": "Space around"
        },
        {
          "value": "space-evenly",
          "label": "Space evenly"
        }
      ],
      "default": "space-between"
    },
    {
      "type": "header",
      "content": "Mobile Layout"
    },
    {
      "type": "range",
      "id": "padding_inline_mobile",
      "label": "Horizontal padding (mobile)",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 20,
      "info": "Internal horizontal spacing on mobile"
    },
    {
      "type": "range",
      "id": "padding_block_mobile",
      "label": "Vertical padding (mobile)",
      "min": 0,
      "max": 48,
      "step": 2,
      "unit": "px",
      "default": 12,
      "info": "Internal vertical spacing on mobile"
    },
    {
      "type": "range",
      "id": "corner_radius_mobile",
      "label": "Corner radius (mobile)",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 24,
      "info": "Rounded corners amount on mobile"
    },
    {
      "type": "range",
      "id": "content_gap_mobile",
      "label": "Gap between groups (mobile)",
      "min": 0,
      "max": 48,
      "step": 2,
      "unit": "px",
      "default": 14,
      "info": "Space between logo and actions on mobile"
    },
    {
      "type": "checkbox",
      "id": "enable_header_animation",
      "label": "Enable header animations",
      "default": true,
      "info": "Smooth transitions and hover effects"
    },
    {
      "type": "range",
      "id": "header_animation_duration",
      "label": "Animation duration (ms)",
      "min": 100,
      "max": 1000,
      "step": 50,
      "unit": "ms",
      "default": 300,
      "info": "Speed of header animations"
    }
  ]
}
{% endschema %}
