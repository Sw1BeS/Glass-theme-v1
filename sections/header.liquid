{% comment %}
  Simple Header: Header IS the glass, content sits on top
  Layer structure: Background -> Orbs -> Glass (header) -> Content
{% endcomment %}

{% liquid
  assign logo_width = section.settings.logo_width | default: '120px'
  assign logo_width_mobile = section.settings.logo_width_mobile | default: '100px'
  assign logo_height = section.settings.logo_height | default: '40px'
  assign logo_height_mobile = section.settings.logo_height_mobile | default: '35px'
  assign menu_items = section.settings.menu.links | default: collections.all
  assign show_search = section.settings.show_search | default: true
  assign show_cart = section.settings.show_cart | default: true
  assign show_account = section.settings.show_account | default: true
  assign sticky_behavior = section.settings.enable_sticky_header | default: 'always'

  assign glass_enabled = section.settings.enable_glass
  if glass_enabled == null
    assign glass_enabled = settings.enable_glass_effect
  endif
  assign glass_tone = section.settings.glass_tone | default: 'auto'
  assign glass_elevation = section.settings.glass_elevation | default: 'medium'

  assign glass_blur_preset = section.settings.glass_blur_preset | default: 'global'
  assign glass_blur = null
  case glass_blur_preset
    when 'soft'
      assign glass_blur = 14
    when 'balanced'
      assign glass_blur = 20
    when 'bold'
      assign glass_blur = 28
  endcase
%}

{% capture header_style %}
  {% if glass_blur != blank %}
    --glass-local-blur: {{ glass_blur }}px;
  {% endif %}
{% endcapture %}
{% assign header_style_trimmed = header_style | strip %}

<header
  class="header glass color-{{ section.settings.color_scheme | default: 'scheme-1' }}"
  data-sticky="{{ sticky_behavior }}"
  {% if glass_enabled %}
    data-glass-tone="{{ glass_tone }}"
    data-glass-elevation="{{ glass_elevation }}"
  {% endif %}
  {% unless header_style_trimmed == '' %}
    style="{{ header_style_trimmed }}"
  {% endunless %}
>
  <!-- Logo - прямо в хедере -->
  <a href="{{ routes.root_url }}" class="logo-link">
    {% if section.settings.logo != blank %}
      <img
        src="{{ section.settings.logo | image_url: width: 400 }}"
        alt="{{ shop.name }}"
        class="logo-image"
        width="400"
        height="auto"
        style="width: {{ logo_width }}; height: {{ logo_height }}; object-fit: contain;"
        loading="eager"
      >
    {% else %}
      <span class="logo-text">{{ shop.name }}</span>
    {% endif %}
  </a>

  <!-- Desktop Menu - прямо в хедере -->
  <nav class="menu-group desktop-only">
    {% if menu_items %}
      {% for link in menu_items %}
        <a href="{{ link.url }}" class="menu-link">{{ link.title }}</a>
      {% endfor %}
    {% else %}
      <a href="{{ routes.root_url }}" class="menu-link">Home</a>
      <a href="{{ routes.collections_url }}" class="menu-link">Collections</a>
      <a href="{{ routes.all_products_collection_url }}" class="menu-link">Products</a>
    {% endif %}
  </nav>

  <!-- Actions - прямо в хедере -->
  <div class="action-group">
    {% if show_search %}
      <a href="{{ routes.search_url }}" class="action-btn" aria-label="Search">🔍</a>
    {% endif %}
    {% if show_account %}
      <a href="{{ routes.account_url }}" class="action-btn" aria-label="Account">👤</a>
    {% endif %}
    {% if show_cart %}
      <a href="{{ routes.cart_url }}" class="action-btn" aria-label="Cart">
        🛒 <span class="cart-count">{{ cart.item_count }}</span>
      </a>
    {% endif %}

    <!-- Theme Toggle Button -->
    {% if section.settings.show_theme_toggle %}
      <button class="theme-fab glass" data-theme-toggle type="button" aria-label="Theme: auto" aria-pressed="mixed">
        🌙
      </button>
    {% endif %}

    <!-- Mobile Menu Toggle -->
    <button class="mobile-toggle mobile-only" aria-label="Toggle menu">☰</button>
  </div>

  <!-- Mobile Menu -->
  <div class="mobile-menu mobile-only">
    {% if menu_items %}
      {% for link in menu_items %}
        <a href="{{ link.url }}" class="mobile-menu-link">{{ link.title }}</a>
      {% endfor %}
    {% else %}
      <a href="{{ routes.root_url }}" class="mobile-menu-link">Home</a>
      <a href="{{ routes.collections_url }}" class="mobile-menu-link">Collections</a>
      <a href="{{ routes.all_products_collection_url }}" class="mobile-menu-link">Products</a>
    {% endif %}
  </div>
</header>

<style>
  /* CSS Variables for dynamic sizing */
  :root {
    --header-logo-width: {{ logo_width }};
    --header-logo-width-mobile: {{ logo_width_mobile }};
    --header-logo-height: {{ logo_height }};
    --header-logo-height-mobile: {{ logo_height_mobile }};
    --header-height: {{ section.settings.header_height | default: 60 }}px;
    --header-width: {{ section.settings.header_width | default: 1200 }}px;
  }

  .header {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 3vw, 1.75rem);
    width: min(100%, var(--header-width, 1200px));
    margin: {{ section.settings.header_margin_top | default: 8 }}px auto {{ section.settings.header_margin_bottom | default: 8 }}px;
    padding: clamp(0.75rem, 2vw, 1rem) clamp(1rem, 3vw, 1.5rem);
    border-radius: clamp(2.25rem, 5vw, 3.5rem);
    position: relative;
    z-index: 10;

    /* Glass Effect */
    background: var(--glass-surface-color, rgba(255, 255, 255, {{ section.settings.header_opacity | default: 85 | times: 0.01 }}));
    backdrop-filter: blur(var(--glass-local-blur, 20px));
    -webkit-backdrop-filter: blur(var(--glass-local-blur, 20px));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.2));
    box-shadow: 0 8px 32px var(--glass-shadow-color, rgba(0, 0, 0, 0.1));

    /* Smooth transitions */
    transition: all 0.3s ease;
  }

  .header > * {
    flex-shrink: 0;
  }

  .header .menu-link {
    flex-shrink: 0;
  }

  .header .menu-group {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  /* Logo - просто контент в div */
  .logo-link {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    transition: transform 0.3s ease;
  }

  .logo-link:hover {
    {% if section.settings.logo_hover_effect %}
      transform: scale({{ section.settings.logo_hover_scale | default: 105 | divided_by: 100.0 }});
    {% endif %}
  }

  .logo-image {
    width: var(--header-logo-width) !important;
    height: var(--header-logo-height) !important;
    object-fit: contain !important;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.15));
    max-width: none !important;
    max-height: none !important;
  }

  .logo-text {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
  }

  /* Menu - прямо в хедере */

  .menu-group {
    flex: 1 1 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: {{ section.settings.menu_item_gap | default: 12 }}px;
  }

  .menu-link {
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    text-decoration: none;
    font-weight: {{ section.settings.menu_font_weight | default: 600 }};
    font-size: {{ section.settings.menu_font_size | default: 16 }}px;
    padding: {{ section.settings.menu_item_padding | default: 8 }}px {{ section.settings.menu_item_padding | default: 8 | times: 1.5 }}px;
    border-radius: 999px;
    transition: all 0.3s ease;
  }

  .menu-link:hover {
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.15));
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    transform: translateY(-2px);
    box-shadow: 0 8px 24px var(--glass-shadow-color, rgba(0, 0, 0, 0.15));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.3));
  }

  /* Actions - прямо в хедере */

  .action-group {
    display: flex;
    align-items: center;
    gap: {{ section.settings.action_button_gap | default: 4 }}px;
    margin-left: auto;
  }

  .action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: {{ section.settings.action_button_size | default: 38 }}px;
    height: {{ section.settings.action_button_size | default: 38 }}px;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    text-decoration: none;
    border-radius: 999px;
    transition: all 0.3s ease;
    position: relative;
    margin: 0 0.2rem;
  }

  .action-btn:hover {
    {% if section.settings.action_button_hover_effect %}
      background: var(--glass-surface-color, rgba(255, 255, 255, 0.15));
      color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
      transform: translateY(-2px);
      box-shadow: 0 8px 24px var(--glass-shadow-color, rgba(0, 0, 0, 0.15));
      border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.3));
    {% endif %}
  }

  .cart-count {
    position: absolute;
    top: -5px;
    right: -5px;
    background: var(--glass-highlight-color, var(--color-primary, #bf8a7a));
    color: white;
    border-radius: 50%;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.7rem;
    font-weight: 600;
  }

  /* Mobile Toggle */
  .mobile-toggle {
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    border-radius: 8px;
    transition: all 0.3s ease;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    font-size: 1.2rem;
  }

  .mobile-toggle:hover {
    background: rgba(255, 255, 255, 0.2);
  }

  /* Mobile Menu */
  .mobile-menu {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.15));
    backdrop-filter: blur(var(--glass-local-blur, 20px));
    -webkit-backdrop-filter: blur(var(--glass-local-blur, 20px));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.2));
    border-top: none;
    border-radius: 0 0 12px 12px;
    padding: 1rem;
    transform: translateY(-100%);
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 8px 32px var(--glass-shadow-color, rgba(0, 0, 0, 0.1));
  }

  .mobile-menu.active {
    transform: translateY(0);
    opacity: 1;
    visibility: visible;
  }

  .mobile-menu-link {
    display: block;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    text-decoration: none;
    font-weight: 500;
    padding: 0.8rem 1rem;
    border-radius: 8px;
    transition: all 0.3s ease;
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.05));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.1));
    margin-bottom: 0.5rem;
  }

  .mobile-menu-link:hover {
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.15));
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    transform: translateX(5px);
    box-shadow: 0 4px 16px var(--glass-shadow-color, rgba(0, 0, 0, 0.1));
  }

  /* Responsive */
  @media screen and (max-width: 749px) {
    .header {
      width: calc(100% - 1rem);
      margin: 0.5rem auto;
      padding: 0.6rem 0.85rem;
      border-radius: 20px;
    }

    .logo-image {
      width: var(--header-logo-width-mobile) !important;
      height: var(--header-logo-height-mobile) !important;
    }

    .logo-text {
      font-size: 1.3rem;
    }

    .action-btn {
      width: 32px;
      height: 32px;
    }
  }

  /* Utility Classes */
  .mobile-only {
    display: none;
  }

  @media screen and (max-width: 920px) {
    .header {
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .desktop-only {
      display: none !important;
    }

    .mobile-only {
      display: block;
    }

    .action-group {
      width: 100%;
      justify-content: flex-end;
    }
  }

  /* Sticky behavior */
  .header[data-sticky="always"] {
    position: fixed;
  }

  .header[data-sticky="scroll-up"] {
    position: fixed;
    transform: translateY(-100%);
  }

  .header[data-sticky="scroll-up"].visible {
    transform: translateY(0);
  }

  .header[data-sticky="never"] {
    position: relative;
    top: auto;
    left: auto;
    right: auto;
    margin: 0.5rem;
  }
</style>

<script>
  // Mobile menu toggle
  document.addEventListener('DOMContentLoaded', function () {
    const mobileToggle = document.querySelector('.mobile-toggle');
    const mobileMenu = document.querySelector('.mobile-menu');

    if (mobileToggle && mobileMenu) {
      mobileToggle.addEventListener('click', function () {
        mobileMenu.classList.toggle('active');
        mobileToggle.classList.toggle('active');
      });
    }

    // Sticky scroll-up behavior
    const header = document.querySelector('.header[data-sticky="scroll-up"]');
    if (header) {
      let lastScrollY = window.scrollY;

      window.addEventListener('scroll', function () {
        const currentScrollY = window.scrollY;

        if (currentScrollY > lastScrollY && currentScrollY > 100) {
          // Scrolling down
          header.classList.remove('visible');
        } else {
          // Scrolling up
          header.classList.add('visible');
        }

        lastScrollY = currentScrollY;
      });
    }

    // Update logo sizes when settings change (for theme editor)
    if (window.Shopify && window.Shopify.theme) {
      window.Shopify.theme.on('section:load', function () {
        const logoImage = document.querySelector('.logo-image');
        if (logoImage) {
          // Force re-render of logo with new size
          logoImage.style.height = 'var(--header-logo-height)';
        }
      });
    }
  });
</script>

{% schema %}
{
  "name": "Simple Header",
  "settings": [
    {
      "type": "header",
      "content": "Header Size & Position"
    },
    {
      "type": "range",
      "id": "header_height",
      "label": "Header Height",
      "min": 40,
      "max": 120,
      "step": 5,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "header_width",
      "label": "Header Width",
      "min": 800,
      "max": 1600,
      "step": 50,
      "unit": "px",
      "default": 1200
    },
    {
      "type": "range",
      "id": "header_margin_top",
      "label": "Top Margin",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "header_margin_bottom",
      "label": "Bottom Margin",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "header",
      "content": "Logo Settings"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "logo_width",
      "label": "Logo Width (Desktop)",
      "default": "120px",
      "info": "Any CSS value: 120px, 200px, 50%, auto, 10rem, etc."
    },
    {
      "type": "text",
      "id": "logo_width_mobile",
      "label": "Logo Width (Mobile)",
      "default": "100px",
      "info": "Any CSS value: 100px, 150px, 50%, auto, 8rem, etc."
    },
    {
      "type": "text",
      "id": "logo_height",
      "label": "Logo Height (Desktop)",
      "default": "40px",
      "info": "Any CSS value: 40px, 80px, 50%, auto, 3rem, etc."
    },
    {
      "type": "text",
      "id": "logo_height_mobile",
      "label": "Logo Height (Mobile)",
      "default": "35px",
      "info": "Any CSS value: 35px, 60px, 50%, auto, 2.5rem, etc."
    },
    {
      "type": "checkbox",
      "id": "logo_hover_effect",
      "label": "Enable Logo Hover Effect",
      "default": true,
      "info": "Add scale animation on hover"
    },
    {
      "type": "select",
      "id": "logo_hover_scale",
      "label": "Logo Hover Scale",
      "options": [
        { "value": "100", "label": "No Scale (100%)" },
        { "value": "105", "label": "Slight Scale (105%)" },
        { "value": "110", "label": "Medium Scale (110%)" },
        { "value": "115", "label": "Strong Scale (115%)" },
        { "value": "120", "label": "Maximum Scale (120%)" }
      ],
      "default": "105"
    },
    {
      "type": "header",
      "content": "Menu Settings"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "range",
      "id": "menu_font_size",
      "label": "Menu Font Size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "select",
      "id": "menu_font_weight",
      "label": "Menu Font Weight",
      "options": [
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "range",
      "id": "menu_item_padding",
      "label": "Menu Item Padding",
      "min": 4,
      "max": 20,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "menu_item_gap",
      "label": "Space Between Menu Items",
      "min": 8,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "header",
      "content": "Action Buttons"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show Search",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_cart",
      "label": "Show Cart",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "Show Account",
      "default": true
    },
    {
      "type": "range",
      "id": "action_button_size",
      "label": "Action Button Size",
      "min": 28,
      "max": 48,
      "step": 2,
      "unit": "px",
      "default": 38
    },
    {
      "type": "range",
      "id": "action_button_gap",
      "label": "Space Between Action Buttons",
      "min": 4,
      "max": 16,
      "step": 2,
      "unit": "px",
      "default": 4
    },
    {
      "type": "checkbox",
      "id": "action_button_hover_effect",
      "label": "Enable Action Button Hover Effects",
      "default": true
    },
    {
      "type": "header",
      "content": "Behavior"
    },
    {
      "type": "select",
      "id": "enable_sticky_header",
      "label": "Sticky Header",
      "options": [
        {
          "value": "always",
          "label": "Always"
        },
        {
          "value": "scroll-up",
          "label": "On Scroll Up"
        },
        {
          "value": "never",
          "label": "Never"
        }
      ],
      "default": "always"
    },
    {
      "type": "header",
      "content": "Color Scheme"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color Scheme",
      "options": [
        {
          "value": "scheme-1",
          "label": "Scheme 1"
        },
        {
          "value": "scheme-2",
          "label": "Scheme 2"
        },
        {
          "value": "scheme-3",
          "label": "Scheme 3"
        },
        {
          "value": "scheme-4",
          "label": "Scheme 4"
        },
        {
          "value": "scheme-5",
          "label": "Scheme 5"
        },
        {
          "value": "scheme-6",
          "label": "Scheme 6"
        },
        {
          "value": "scheme-7",
          "label": "Scheme 7"
        }
      ],
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Glass styling"
    },
    {
      "type": "checkbox",
      "id": "enable_glass",
      "label": "Use glassmorphism background",
      "default": true,
      "info": "Falls back to the global glass setting when left unchecked in existing themes."
    },
    {
      "type": "select",
      "id": "glass_tone",
      "label": "Glass tone",
      "options": [
        {
          "value": "auto",
          "label": "Auto"
        },
        {
          "value": "light",
          "label": "Light"
        },
        {
          "value": "dark",
          "label": "Dark"
        }
      ],
      "default": "auto"
    },
    {
      "type": "select",
      "id": "glass_elevation",
      "label": "Glass elevation",
      "options": [
        {
          "value": "low",
          "label": "Low"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "high",
          "label": "High"
        }
      ],
      "default": "medium"
    },
    {
      "type": "select",
      "id": "glass_blur_preset",
      "label": "Blur strength",
      "options": [
        {
          "value": "global",
          "label": "Use theme default"
        },
        {
          "value": "soft",
          "label": "Soft (14px)"
        },
        {
          "value": "balanced",
          "label": "Balanced (20px)"
        },
        {
          "value": "bold",
          "label": "Bold (28px)"
        }
      ],
      "default": "global"
    },
    {
      "type": "header",
      "content": "Advanced Settings"
    },
    {
      "type": "range",
      "id": "header_opacity",
      "label": "Header Background Opacity",
      "min": 10,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 85,
      "info": "Controls the transparency of the header background"
    },
    {
      "type": "checkbox",
      "id": "show_theme_toggle",
      "label": "Show Theme Toggle Button",
      "default": true,
      "info": "Display the theme switcher button in the header"
    }
  ]
}
{% endschema %}
