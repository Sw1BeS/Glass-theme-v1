{% comment %}
  Hero Section - Main banner with image background and content overlay
  Features: Full-screen background, glass effect content blocks, responsive design
{% endcomment %}

{% liquid
  assign glass_enabled = section.settings.enable_glass
  if glass_enabled == null
    assign glass_enabled = settings.enable_glass_effect
  endif
  assign glass_tone = section.settings.glass_tone | default: 'auto'
  assign glass_elevation = section.settings.glass_elevation | default: 'medium'
%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} hero-section color-{{ section.settings.color_scheme }} spacing-style"
  {% if glass_enabled %}
    data-glass-tone="{{ glass_tone }}"
    data-glass-elevation="{{ glass_elevation }}"
  {% endif %}
>
  <!-- Background Image -->
  <div class="hero-background">
    {%- if section.settings.image_1 != blank -%}
      <!-- Desktop Image -->
      <div class="hero-image-desktop">
        {{
          section.settings.image_1
          | image_url: width: 1920
          | image_tag:
            loading: 'eager',
            sizes: '(min-width: 768px) 100vw, 0px',
            widths: '768, 1024, 1280, 1920',
            class: 'hero-bg-image'
        }}
      </div>
    {%- endif -%}

    {%- if section.settings.image_1_mobile != blank -%}
      <!-- Mobile Image -->
      <div class="hero-image-mobile">
        {{
          section.settings.image_1_mobile
          | image_url: width: 768
          | image_tag:
            loading: 'eager',
            sizes: '(max-width: 767px) 100vw, 0px',
            widths: '375, 414, 768',
            class: 'hero-bg-image'
        }}
      </div>
    {%- elsif section.settings.image_1 != blank -%}
      <!-- Fallback to desktop image for mobile -->
      <div class="hero-image-mobile">
        {{
          section.settings.image_1
          | image_url: width: 768
          | image_tag:
            loading: 'eager',
            sizes: '(max-width: 767px) 100vw, 0px',
            widths: '375, 414, 768',
            class: 'hero-bg-image'
        }}
      </div>
    {%- endif -%}
  </div>

  <!-- Content Overlay -->
  <div class="hero-content">
    <div class="hero-container hero-container--{{ section.settings.group_alignment }}">
      <!-- Hero Content Group -->
      <div
        class="hero-content-group glass"
        data-group-id="hero-content"
        style="
          max-width: {{ section.settings.group_max_width }}px;
          padding: {{ section.settings.group_padding }}rem;
        "
      >
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'text' -%}
              <div class="hero-text-block" {{ block.shopify_attributes }}>
                {{ block.settings.text }}
              </div>
            {%- when 'button' -%}
              {%- if block.settings.label != blank -%}
                <div class="hero-button-block" {{ block.shopify_attributes }}>
                  <a
                    href="{{ block.settings.link | default: '#' }}"
                    class="hero-button"
                    {% if block.settings.open_in_new_tab %}
                      target="_blank" rel="noopener"
                    {% endif %}
                  >
                    {{ block.settings.label }}
                  </a>
                </div>
              {%- endif -%}
          {%- endcase -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

{% stylesheet %}
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .hero-image-desktop {
    display: block;
  }

  .hero-image-mobile {
    display: none;
  }

  @media (max-width: 767px) {
    .hero-image-desktop {
      display: none;
    }

    .hero-image-mobile {
      display: block;
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 1200px;
    padding: 2rem;
  }

  .hero-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    gap: 2rem;
  }

  .hero-container--left {
    align-items: flex-start;
    text-align: left;
  }

  .hero-container--right {
    align-items: flex-end;
    text-align: right;
  }

  /* Hero Content Group - Glass Container */
  .hero-content-group {
    background: var(--glass-surface-color, rgba(255, 255, 255, 0.78));
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.4));
    border-radius: 20px;
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    box-shadow: 0 24px 48px var(--glass-shadow-color, rgba(17, 23, 34, 0.14));
    padding: 3rem;
    max-width: 700px;
    width: 100%;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    position: relative;
    isolation: isolate;
  }

  .hero-content-group::after {
    content: '';
    position: absolute;
    inset: 1px;
    border-radius: inherit;
    background: linear-gradient(135deg, var(--glass-highlight-color, #bf8a7a) 14%, transparent 65%);
    opacity: 0.45;
    pointer-events: none;
    z-index: -1;
  }

  .hero-content-group:hover {
    transform: translateY(-3px);
    box-shadow: 0 28px 64px rgba(17, 23, 34, 0.16);
  }

  /* Text blocks inside group */
  .hero-text-block {
    background: transparent;
    border: none;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    box-shadow: none;
    padding: 0;
    max-width: none;
    transition: none;
  }

  .hero-text-block:hover {
    transform: none;
    box-shadow: none;
  }

  .hero-text-block h1,
  .hero-text-block h2,
  .hero-text-block h3,
  .hero-text-block h4,
  .hero-text-block h5,
  .hero-text-block h6 {
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    margin: 0 0 1rem 0;
    font-weight: 700;
    font-size: clamp(2rem, 5vw, 3.5rem);
  }

  .hero-text-block p {
    color: var(--glass-foreground-muted, var(--color-foreground-muted, #666666));
    margin: 0 0 1rem 0;
    font-size: 1.1rem;
    line-height: 1.6;
  }

  .hero-text-block p:last-child {
    margin-bottom: 0;
  }

  .hero-button-block {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 1rem;
  }

  /* Button blocks inside group */
  .hero-content-group .hero-button-block {
    margin-top: 0.5rem;
  }

  .hero-content-group .hero-button-block:first-of-type {
    margin-top: 1.5rem;
  }

  .hero-button {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.85rem 1.8rem;
    border-radius: 999px;
    text-decoration: none;
    font-weight: 600;
    color: white;
    background: linear-gradient(
      135deg,
      var(--glass-highlight-color, var(--color-primary, #bf8a7a)) 0%,
      var(--color-primary, #bf8a7a) 100%
    );
    border: 1px solid var(--glass-surface-border-color, rgba(255, 255, 255, 0.4));
    transition: all 0.3s ease;
  }

  .hero-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 24px 48px
      color-mix(in srgb, var(--glass-highlight-color, var(--color-primary, #bf8a7a)) 32%, transparent);
  }

  /* Secondary button styling */
  .hero-button[href*='#shopify-section'] {
    background: transparent;
    color: var(--glass-foreground-strong, var(--color-foreground, #2c2c2c));
    border: 2px solid var(--glass-highlight-color, var(--color-primary, #bf8a7a));
  }

  .hero-button[href*='#shopify-section']:hover {
    background: var(--glass-highlight-color, var(--color-primary, #bf8a7a));
    color: white;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .hero-content {
      padding: 1rem;
    }

    .hero-container {
      gap: 1.5rem;
    }

    .hero-content-group {
      padding: 2rem;
      max-width: 100%;
      border-radius: 16px;
    }

    .hero-text-block h1,
    .hero-text-block h2,
    .hero-text-block h3,
    .hero-text-block h4,
    .hero-text-block h5,
    .hero-text-block h6 {
      font-size: clamp(1.8rem, 6vw, 2.5rem);
    }

    .hero-text-block p {
      font-size: 1rem;
    }

    .hero-button-block {
      flex-direction: column;
      align-items: center;
      gap: 0.75rem;
    }

    .hero-button {
      width: 100%;
      max-width: 280px;
      justify-content: center;
      padding: 0.75rem 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .hero-content-group {
      padding: 1.5rem;
      border-radius: 12px;
    }

    .hero-text-block h1,
    .hero-text-block h2,
    .hero-text-block h3,
    .hero-text-block h4,
    .hero-text-block h5,
    .hero-text-block h6 {
      font-size: clamp(1.5rem, 8vw, 2rem);
    }

    .hero-button {
      padding: 0.7rem 1.2rem;
      font-size: 0.9rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.hero",
  "tag": "section",
  "class": "hero-wrapper section-wrapper",
  "blocks": [
    {
      "type": "text"
    },
    {
      "type": "button"
    }
  ],
  "disabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:content.media"
    },
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "t:settings.image (Desktop)",
      "info": "Recommended: 1920x1080px"
    },
    {
      "type": "image_picker",
      "id": "image_1_mobile",
      "label": "t:settings.image (Mobile)",
      "info": "Recommended: 768x1024px (vertical)"
    },
    {
      "type": "header",
      "content": "t:content.layout"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "full-width"
    },
    {
      "type": "header",
      "content": "t:content.appearance"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Content Group Settings"
    },
    {
      "type": "select",
      "id": "group_alignment",
      "label": "Group Alignment",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "group_max_width",
      "label": "Group Max Width",
      "min": 400,
      "max": 800,
      "step": 50,
      "unit": "px",
      "default": 700
    },
    {
      "type": "range",
      "id": "group_padding",
      "label": "Group Padding",
      "min": 1,
      "max": 5,
      "step": 0.5,
      "unit": "rem",
      "default": 3
    },
    {
      "type": "checkbox",
      "id": "enable_glass",
      "label": "Enable Glass Effect",
      "default": true
    },
    {
      "type": "select",
      "id": "glass_tone",
      "label": "Glass Tone",
      "options": [
        {
          "value": "auto",
          "label": "Auto"
        },
        {
          "value": "light",
          "label": "Light"
        },
        {
          "value": "dark",
          "label": "Dark"
        }
      ],
      "default": "auto"
    },
    {
      "type": "select",
      "id": "glass_elevation",
      "label": "Glass Elevation",
      "options": [
        {
          "value": "low",
          "label": "Low"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "high",
          "label": "High"
        }
      ],
      "default": "medium"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "t:names.hero",
      "category": "t:categories.banners",
      "settings": {
        "section_width": "full-width",
        "color_scheme": "scheme-1",
        "group_alignment": "center",
        "group_max_width": 700,
        "group_padding": 3,
        "enable_glass": true,
        "glass_tone": "auto",
        "glass_elevation": "medium",
        "padding-block-start": 0,
        "padding-block-end": 0
      },
      "blocks": {
        "text_1": {
          "type": "text",
          "settings": {
            "text": "<h1>The Perfect Brow Duo</h1>"
          }
        },
        "text_2": {
          "type": "text",
          "settings": {
            "text": "<p>Everything you need for flawless brows, every day.</p>"
          }
        },
        "button_1": {
          "type": "button",
          "settings": {
            "label": "Shop The Duo - $34.99",
            "link": "/products/the-duo"
          }
        },
        "button_2": {
          "type": "button",
          "settings": {
            "label": "Learn How to Use",
            "link": "#shopify-section-how_to"
          }
        }
      },
      "block_order": ["text_1", "text_2", "button_1", "button_2"]
    }
  ]
}
{% endschema %}
